# Roblox Studio place lock — created while a .rbxl is open, transient
*.rbxl.lock

# Rojo / build output (place is built to a temp path, not committed)
/build/

# Rojo sourcemap — auto-generated by `rojo sourcemap` (Luau LSP watcher);
# regenerated on demand, so it doesn't belong in version control
sourcemap.json

# macOS
.DS_Store
